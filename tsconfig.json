{
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue", "src/**/*.js"], // Agregar "src/**/*.js" para incluir archivos JavaScript
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "allowJs": true // Permitir archivos JavaScript
  },

  "references": [
    {
      "path": "./tsconfig.config.json"
    }
  ]
}